# Optional: absolute public URL of the site, used to resolve OpenGraph and
# Twitter image paths. When unset, Vercel deploys fall back to VERCEL_URL
# automatically and local dev falls back to http://localhost:3000.
# Set this to your custom domain once linked (e.g. https://brief.example.com).
NEXT_PUBLIC_SITE_URL=
